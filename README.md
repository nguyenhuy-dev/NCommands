# Clone find command 
A cSharp console application which can search all lines in a text file. Those lines are containing a keyword with many options such as: ignore case, lower case, not contain,...

A light version of find command on windows.

### Demo for some pattens
- Options Builder
- Factory
